SIMPLE mailing_list page that grabs user inputed email and adds it to a database(local). Displays the total number of users in the database.

*used to learn how to conenct mysql and node using express.*